# Project specification pointer

The complete, authoritative implementation specification is [GeoConflux_Plan.md](GeoConflux_Plan.md).

This short file exists so tooling that expects `PROJECT_SPEC.md` has an unambiguous entry point. Architectural changes are recorded in `docs/adr/`; an explicit newer ADR takes precedence over the plan.
