# AI evaluation fixtures

This directory deliberately has no model claims or metrics in Sprint 1. Sprint 3 will add labelled, sanitised inputs and deterministic mock-provider expectations. Any reported evaluation result must come from the evaluation test run that produced it.
