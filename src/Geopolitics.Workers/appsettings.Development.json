{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "Ai": {
    "_comment": "Development runs against a local Ollama daemon, so a foreign-language report is actually translated instead of being shown in its source language. Needs `ollama serve` and `ollama pull qwen3.5:4b`; if the daemon is not running, enrichment fails cleanly and the deterministic keyword classification stands.",
    "Provider": "Ollama",
    "Model": "qwen3.5:4b"
  }
}
